{
  "pkg": "dreamcontext-dashboard",
  "globalName": "DreamContextDS",
  "projectId": "81c09a7e-7c02-470b-bb9f-0727c20f3b91",
  "shape": "package",
  "entry": "dashboard/_ds_entry.tsx",
  "srcDir": "src",
  "readmeHeader": ".design-sync/conventions.md",
  "runtimeFontPrefixes": [
    "Inter",
    "Plus Jakarta Sans",
    "JetBrains Mono",
    "Sometype Mono"
  ],
  "componentSrcMap": {
    "StatusBadge": "src/components/council/StatusBadge.tsx",
    "ModelBadge": "src/components/council/ModelBadge.tsx",
    "StatTile": "src/components/council/StatTile.tsx",
    "PersonaAvatar": "src/components/council/PersonaAvatar.tsx",
    "TaskCard": "src/components/tasks/TaskCard.tsx",
    "KanbanColumn": "src/components/tasks/KanbanColumn.tsx",
    "SubGroupSection": "src/components/tasks/SubGroupSection.tsx",
    "MiniCalendar": "src/components/tasks/MiniCalendar.tsx",
    "SearchableSelect": "src/components/tasks/SearchableSelect.tsx",
    "MultiSelectFilter": "src/components/tasks/MultiSelectFilter.tsx",
    "FilterPopover": "src/components/tasks/FilterPopover.tsx",
    "CustomFieldInput": "src/components/tasks/CustomFieldInput.tsx",
    "ActivityHeatmap": "src/components/tasks/ActivityHeatmap.tsx",
    "EisenhowerMatrix": "src/components/tasks/EisenhowerMatrix.tsx",
    "RiceScatter": "src/components/tasks/RiceScatter.tsx",
    "VersionFilter": "src/components/tasks/VersionFilter.tsx"
  },
  "dtsPropsFor": {
    "StatusBadge": "  /** Council run status string, e.g. \"round_1_running\", \"synthesizing\", \"complete\", \"created\". */\n  status: string;\n  size?: 'sm' | 'md';",
    "ModelBadge": "  /** Model name, e.g. \"claude\", \"gpt\", \"gemini\"; the badge hue is hashed from it. */\n  model: string;",
    "StatTile": "  value: number | string;\n  label: string;\n  tone?: 'default' | 'warning' | 'dissent' | 'brand';",
    "PersonaAvatar": "  /** Persona slug; drives the gradient hue and the displayed glyph. */\n  slug: string;\n  /** Square avatar size in px. Default 36. */\n  size?: number;\n  title?: string;\n  onClick?: () => void;",
    "TaskCard": "  task: { slug: string; id: string; name: string; description: string; priority: 'critical' | 'high' | 'medium' | 'low'; urgency: 'critical' | 'high' | 'medium' | 'low'; status: 'todo' | 'in_progress' | 'in_review' | 'completed'; created_at: string; updated_at: string; tags: string[]; parent_task: string | null; related_feature: string | null; version: string | null; start_date?: string | null; due_date?: string | null; assignee?: string | null; custom_fields?: Record<string, string | number | null>; rice: { reach: number | null; impact: number | null; confidence: number | null; effort: number | null; score: number | null } | null; why: string; user_stories: string; acceptance_criteria: string; constraints: string; technical_details: string; notes: string; changelog: string; sections: string[]; body: string };\n  onClick: () => void;\n  onDragStart: (e: React.DragEvent) => void;\n  /** Right-click handler (context menu). */\n  onContextMenu?: (e: React.MouseEvent) => void;\n  /** Dimmed \"ghost\" state while this card is the one being dragged. */\n  dragging?: boolean;",
    "KanbanColumn": "  title: string;\n  status: string;\n  tasks: Array<{ slug: string; id: string; name: string; description: string; priority: 'critical' | 'high' | 'medium' | 'low'; urgency: 'critical' | 'high' | 'medium' | 'low'; status: 'todo' | 'in_progress' | 'in_review' | 'completed'; created_at: string; updated_at: string; tags: string[]; parent_task: string | null; related_feature: string | null; version: string | null; start_date?: string | null; due_date?: string | null; assignee?: string | null; custom_fields?: Record<string, string | number | null>; rice: { reach: number | null; impact: number | null; confidence: number | null; effort: number | null; score: number | null } | null; why: string; user_stories: string; acceptance_criteria: string; constraints: string; technical_details: string; notes: string; changelog: string; sections: string[]; body: string }>;\n  count: number;\n  /** CSS custom-property name used for the column accent color. */\n  colorVar: string;\n  onTaskClick: (task: KanbanColumnProps['tasks'][number]) => void;\n  onTaskContextMenu?: (task: KanbanColumnProps['tasks'][number], e: React.MouseEvent) => void;\n  onDrop: (slug: string, newStatus: string) => void;\n  staggerIndex?: number;\n  subGroups?: Array<{ key: string; label: string; color?: string; tasks: KanbanColumnProps['tasks'] }>;",
    "SubGroupSection": "  label: string;\n  color?: string;\n  tasks: Array<{ slug: string; id: string; name: string; description: string; priority: 'critical' | 'high' | 'medium' | 'low'; urgency: 'critical' | 'high' | 'medium' | 'low'; status: 'todo' | 'in_progress' | 'in_review' | 'completed'; created_at: string; updated_at: string; tags: string[]; parent_task: string | null; related_feature: string | null; version: string | null; start_date?: string | null; due_date?: string | null; assignee?: string | null; custom_fields?: Record<string, string | number | null>; rice: { reach: number | null; impact: number | null; confidence: number | null; effort: number | null; score: number | null } | null; why: string; user_stories: string; acceptance_criteria: string; constraints: string; technical_details: string; notes: string; changelog: string; sections: string[]; body: string }>;\n  onTaskClick: (task: SubGroupSectionProps['tasks'][number]) => void;\n  onTaskContextMenu?: (task: SubGroupSectionProps['tasks'][number], e: React.MouseEvent) => void;\n  onDragStart: (e: React.DragEvent, task: SubGroupSectionProps['tasks'][number]) => void;",
    "MiniCalendar": "  dateField: 'created_at' | 'updated_at';\n  /** ISO date (YYYY-MM-DD) or empty string for no bound. */\n  dateFrom: string;\n  dateTo: string;\n  onDateFieldChange: (field: 'created_at' | 'updated_at') => void;\n  onDateFromChange: (date: string) => void;\n  onDateToChange: (date: string) => void;",
    "SearchableSelect": "  value: string | null;\n  options: Array<{ value: string; label: string; hint?: string }>;\n  placeholder?: string;\n  searchPlaceholder?: string;\n  /** null = cleared. */\n  onChange: (value: string | null) => void;\n  /** Offer \"use the typed text\" when it matches no option. */\n  allowCustom?: boolean;\n  clearLabel?: string;",
    "MultiSelectFilter": "  id: string;\n  label: string;\n  options: Array<{ value: string; label: string; color?: string }>;\n  selected: string[];\n  onChange: (values: string[]) => void;\n  isOpen: boolean;\n  onToggle: () => void;\n  onClose: () => void;",
    "FilterPopover": "  trigger: React.ReactNode;\n  content: React.ReactNode;\n  isOpen: boolean;\n  onClose: () => void;\n  align?: 'left' | 'right';\n  width?: number;",
    "CustomFieldInput": "  field: { name: string; key: string; type: 'text' | 'number' | 'select' | 'date'; required?: boolean; options?: string[]; sync: Array<'clickup' | 'github'>; prompt?: string; ask?: boolean };\n  value: string | number | null | undefined;\n  /** Live change as the user types/picks. */\n  onChange: (value: string | number | null) => void;\n  /** Commit for persistence (select/date on pick, text/number on blur). */\n  onCommit?: (value: string | number | null) => void;\n  className?: string;",
    "ActivityHeatmap": "  tasks: Array<{ slug: string; id: string; name: string; description: string; priority: 'critical' | 'high' | 'medium' | 'low'; urgency: 'critical' | 'high' | 'medium' | 'low'; status: 'todo' | 'in_progress' | 'in_review' | 'completed'; created_at: string; updated_at: string; tags: string[]; parent_task: string | null; related_feature: string | null; version: string | null; start_date?: string | null; due_date?: string | null; assignee?: string | null; custom_fields?: Record<string, string | number | null>; rice: { reach: number | null; impact: number | null; confidence: number | null; effort: number | null; score: number | null } | null; why: string; user_stories: string; acceptance_criteria: string; constraints: string; technical_details: string; notes: string; changelog: string; sections: string[]; body: string }>;\n  onSelectDay?: (date: string, tasks: ActivityHeatmapProps['tasks']) => void;",
    "EisenhowerMatrix": "  tasks: Array<{ slug: string; id: string; name: string; description: string; priority: 'critical' | 'high' | 'medium' | 'low'; urgency: 'critical' | 'high' | 'medium' | 'low'; status: 'todo' | 'in_progress' | 'in_review' | 'completed'; created_at: string; updated_at: string; tags: string[]; parent_task: string | null; related_feature: string | null; version: string | null; start_date?: string | null; due_date?: string | null; assignee?: string | null; custom_fields?: Record<string, string | number | null>; rice: { reach: number | null; impact: number | null; confidence: number | null; effort: number | null; score: number | null } | null; why: string; user_stories: string; acceptance_criteria: string; constraints: string; technical_details: string; notes: string; changelog: string; sections: string[]; body: string }>;\n  onTaskClick: (task: EisenhowerMatrixProps['tasks'][number]) => void;\n  /** Omit to render a read-only matrix; fires when a task is dragged to a new quadrant. */\n  onTaskMove?: (slug: string, updates: { priority?: 'critical' | 'high' | 'medium' | 'low'; urgency?: 'critical' | 'high' | 'medium' | 'low' }) => void;",
    "RiceScatter": "  tasks: Array<{ slug: string; id: string; name: string; description: string; priority: 'critical' | 'high' | 'medium' | 'low'; urgency: 'critical' | 'high' | 'medium' | 'low'; status: 'todo' | 'in_progress' | 'in_review' | 'completed'; created_at: string; updated_at: string; tags: string[]; parent_task: string | null; related_feature: string | null; version: string | null; start_date?: string | null; due_date?: string | null; assignee?: string | null; custom_fields?: Record<string, string | number | null>; rice: { reach: number | null; impact: number | null; confidence: number | null; effort: number | null; score: number | null } | null; why: string; user_stories: string; acceptance_criteria: string; constraints: string; technical_details: string; notes: string; changelog: string; sections: string[]; body: string }>;\n  onTaskClick: (task: RiceScatterProps['tasks'][number]) => void;",
    "VersionFilter": "  /** Pre-sorted: current -> planning -> unregistered -> backlog -> released. */\n  items: Array<{ value: string; status: 'planning' | 'released' | 'unregistered' | 'backlog'; date?: string; isCurrent: boolean; taskCount: number }>;\n  selected: string[];\n  onChange: (values: string[]) => void;\n  currentVersion: string | null;\n  isOpen: boolean;\n  onToggle: () => void;\n  onClose: () => void;\n  onSetCurrent: (version: string) => void;\n  onComplete: (item: VersionFilterProps['items'][number]) => void;\n  /** Version with an in-flight mutation (disables its actions). */\n  busyVersion?: string | null;"
  }
}