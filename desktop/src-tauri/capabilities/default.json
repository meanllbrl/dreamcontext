{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the dreamcontext desktop shell. Grants the tightest permission set needed: core defaults, shell spawn (scoped to node only), shell kill, and updater.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "node",
          "cmd": "node"
        }
      ]
    },
    "shell:allow-kill",
    "updater:default"
  ]
}
