---
name: active-decisions
type: memory
updated: '2026-07-30T00:00:00Z'
---

## Active Memory

> Ship events live in `core/CHANGELOG.json` — browse it with `dreamcontext changelog list`, search it with `dreamcontext memory recall "<query>"`.

## Technical Decisions

> Archived to knowledge/archive/2.memory-2026-h2.md: 60+ decisions, 2026-05-23 → 2026-07-27. Findable via `dreamcontext memory recall`.

- **★★★ Memory ≤4,000 chars renders VERBATIM (2026-07-29)**: sleep is memory's compressor — it distills to the ceiling and archives past it, so render-time rungs were double taxation. At/under CORE_FILE_CHAR_CEILING this file renders IN FULL at every budget level + a recall pointer; over-ceiling vaults keep the 3-rung ladder + doctor error. So: hold it ≤4,000, load-bearing decisions only.

- **★★★ Soul is constitution-only; enforcement is systemic, not render-time (2026-07-30)**: soul = always-valid rules + agent behavior/voice, rendered FULL and VERBATIM with no rungs (a big soul is correctly big). Everything CONDITIONAL moves to `knowledge/patterns/`, which own a hard-banner block at every rung. Enforced by a char ceiling + the 0.23.0 soul-split migration, NOT by compressing at render. Supersedes "budget ladder unwinnable on mature vaults" (2026-07-28) — the ladder was never the fix. Proof: soul 32,912→3,983; that vault 53,668→16,320, inline, no banner.

- **★★★ Level-0 density doctrine + accounting (2026-07-30)**: the DEFAULT render must itself be compact — never fix at the rungs what level 0 should not have emitted. Only PINNED knowledge carries descriptions at any level; the rest is named, never summarized. A rung ending in a bare count is a BLINDING (supersedes 2026-07-28 bare-count): every inventory section owes an ACCOUNTING — detailed + named + counted = total — plus a search/browse footer at every rung incl. floors.

- **★★ Behavioral rules enforce in hook, not skill body (2026-07-28)**: Rule 13 was SKILL.md-only, unenforced when skill didn't load. UserPromptSubmit hook ALWAYS fires → that's where must-not-miss goes. Hook appends insight directive (`lab show`, "no API/MCP") to every recall hit. Pattern: enforcement in hook (always), not skill (conditional). Source: file:src/cli/commands/hook.ts.

- **★★ ESM cache-bust freshens the entry, not its imports (2026-07-28)**: `import(url+'?t=<bust>')` leaves the entry's own `import './lib'` pinned in Node's registry — invisible in CLI, permanent in desktop. Busting a graph's root is not busting the graph; isolate the process. Source: file:src/lib/lab/adapters/script-child.ts; #242.

- **★★★ PID lockfile for concurrent CLI RMW (2026-07-23)**: Concurrent subcommands on shared JSON MUST serialize. O_EXCL create (atomic), bounded spin (50ms/3s), stale-TTL (10s), loud timeout. Without: race, silent drop. Source: file:src/lib/council.ts; changelog:2026-07-23|council.

## Known Issues

- **★★★ 0.23.0 migrations stay dormant until package.json bumps to 0.23.0 (2026-07-29)**: applied range is half-open `(setupVersion, cliVersion]`, so people-first AND soul-split cannot fire on a 0.22.0 CLI. Whoever cuts the release MUST bump the version with the feature.

- **Full suite on saturated machine times out 11 tests (2026-07-27, recurring)**: `npm test` on high CPU load fails 11/135 integration tests at 5s limit. Tests spawn CLI; spawn itself can exceed window. Workaround: run suite alone or on lightly-loaded machine.

- **RELEASES.json indent drift → 1000s-line diffs (2026-07-25, ★★ recurring)**: a 1-space rewrite instead of the CLI's 2-space turned a 3-line change into a 6,700-line diff. Match 2-space; never reformat with non-CLI tooling.

- **`releases add --yes` over-sweeps (2026-07-07, recurring)**: Auto-discovery sweeps far more tasks/changelog/features than belong. v0.13.1 swept 105 tasks for a 2-feature patch. Manual trim after each add. Avoid `--yes` or correct afterward.
