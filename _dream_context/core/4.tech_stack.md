---
name: tech-stack
type: tech
updated: "2026-05-23"
summary: "TypeScript CLI (Node 18+, ESM) + Node HTTP server + React 19/Vite 6 dashboard. CLI bundled by tsup; dashboard built by Vite, output copied to dist/dashboard/. v0.5.0+ adds goal-skill pack, BM25/Haiku recall, curl|sh install, server security hardening. v0.6 adds vault registry, control-plane routes, React Settings/Packs/UpdateBadge pages, dashboard --vault, and Tauri 2.x desktop shell scaffold."
---

## Tech Stack

TypeScript 5.7, Node.js >= 18, ESM-only npm package. CLI + local HTTP server + React SPA dashboard.

## Architecture Overview

Single-binary CLI tool that can also serve a local web dashboard. No external services. All state lives on the local filesystem in `_dream_context/` directories within user projects.

**Source layout**:
```
src/
├── cli/
│   ├── index.ts              # Entry point, banner, interactive mode, program setup
│   ├── interactive.ts        # readline REPL for no-args mode
│   └── commands/
│       ├── init.ts           # dreamcontext init (--multi-product flag, per-product data-structures)
│       ├── setup.ts          # dreamcontext setup (one-shot init+install+instructions, v0.4.0)
│       ├── core.ts           # dreamcontext core {changelog,releases}
│       ├── features.ts       # dreamcontext features {create,update_properties,insert}
│       ├── tasks.ts          # dreamcontext tasks {create,update,complete,log}
│       ├── knowledge.ts      # dreamcontext knowledge {create,update}
│       ├── sleep.ts          # dreamcontext sleep {status,add,done,debt}
│       ├── hook.ts           # dreamcontext hook {session-start,stop}
│       ├── snapshot.ts       # dreamcontext snapshot (plain-text context dump; injects per-product knowledge)
│       ├── dashboard.ts      # dreamcontext dashboard [--vault <name|path>] (starts HTTP server + opens browser)
│       ├── vaults.ts         # dreamcontext vaults {add,list,remove} (global vault registry; v0.6.0)
│       ├── update.ts         # dreamcontext update (manifest-aware diff + prune)
│       ├── upgrade.ts        # dreamcontext upgrade [--check] (in-session update nudge; v0.5.0)
│       ├── memory.ts         # dreamcontext memory {recall,remember,update,delete,list,status} (v0.4.0+; BM25 over curated corpus)
│       └── install-skill.ts  # dreamcontext install-skill (copies skill+agents, records to manifest)
├── server/
│   ├── index.ts              # createServer(), startDashboardServer(), route registration
│   ├── router.ts             # URL pattern matching with :param extraction
│   ├── middleware.ts         # JSON parsing, CORS, CSRF guard (loopback Origin check on mutating requests)
│   ├── safe-path.ts          # safeChildPath() — path-traversal guard for slug-to-path joins (v0.5.1+)
│   ├── static.ts             # Serves dashboard/dist/ static files
│   ├── change-tracker.ts     # recordDashboardChange() writes to .sleep.json dashboard_changes
│   └── routes/
│       ├── tasks.ts          # GET/POST/PATCH/DELETE /api/tasks
│       ├── sleep.ts          # GET/POST /api/sleep
│       ├── core.ts           # GET/PUT /api/core (safeChildPath on :filename)
│       ├── knowledge.ts      # GET/PATCH /api/knowledge
│       ├── features.ts       # GET /api/features
│       ├── changelog.ts      # GET /api/changelog, GET /api/releases/unreleased, GET /api/releases/:version, POST /api/releases
│       ├── config.ts         # GET/PATCH /api/config (strict-pick {platforms,packs}; v0.6.0)
│       ├── packs.ts          # GET /api/packs (v0.6.0)
│       ├── version-check.ts  # GET /api/version-check (cache-only; v0.6.0)
│       ├── vaults.ts         # GET /api/vaults (read-only list; v0.6.0)
│       └── health.ts         # GET /api/health
└── lib/
    ├── context-path.ts       # _dream_context/ root resolution (walk-up, MAX_WALK_UP=5)
    ├── frontmatter.ts        # gray-matter wrappers
    ├── markdown.ts           # Section-level operations: readSection, insertToSection, listSections
    ├── json-file.ts          # JSON array + object CRUD
    ├── search.ts             # Keyword search
    ├── id.ts                 # generateId, slugify, today
    ├── format.ts             # CLI output formatting
    ├── core-index.ts         # Scans core/[3-9]* files for snapshot
    ├── knowledge-index.ts    # Scans knowledge/*.md frontmatter for snapshot
    ├── release-discovery.ts  # Auto-discovers unreleased tasks/features/changelog for releases add
    ├── release-backpopulate.ts  # Sets released_version on features when bundled into a release
    ├── manifest.ts           # Install manifest read/write/diff (v0.4.0; tracks all system-managed files)
    ├── recall.ts             # BM25 ranker + corpus loader for `dreamcontext memory recall` (v0.4.0+; no persistent index, in-memory rebuild per query)
    ├── recall-query-extractor.ts  # haikuRecall(): single Haiku CLI call returns 0-3 doc keys; falls back to BM25 (v0.5.0)
    ├── version-check.ts      # upgrade check: network call ≥once/24h from UserPromptSubmit hook (cache-only in request path; v0.5.0)
    ├── vaults.ts             # Global vault registry (~/.dreamcontext/vaults.json) + resolveVaultContextRoot() (v0.6.0)
    ├── catalog.ts            # loadCatalog() + catalog types (extracted from install-skill.ts to keep @inquirer out of server bundle; v0.6.0)
    └── setup-config.ts       # .config.json read/write (platforms, packs, multiProduct, setupVersion)

dashboard/                    # Separate package (own package.json)
├── src/
│   ├── App.tsx               # React root with QueryClient, Theme, I18n providers, ErrorBoundary
│   ├── api/client.ts         # Fetch wrapper with res.ok checks and error parsing
│   ├── styles/tokens.css     # Design tokens (colors, spacing, typography)
│   ├── styles/global.css     # Base styles, brand gradient background, stagger entrance animations
│   ├── pages/                # Tasks, Sleep, Core, Knowledge, Features, Settings, Packs pages
│   ├── components/           # KanbanBoard, TaskDetail, CoreEditor, KnowledgeCard, SqlPreview, UpdateBadge, etc.
│   ├── lib/sql-parser.ts     # Parses SQL CREATE TABLE + comment-based schemas, extracts entities/fields/relationships
│   ├── hooks/                # TanStack Query hooks (useTasks, useSleep, useCore, etc.)
│   └── contexts/             # ThemeContext, I18nContext
└── dist/                     # Vite build output (copied to dist/dashboard/ by tsup)
```

**Distributed assets** (not TypeScript, shipped alongside dist):
```
skill/
└── SKILL.md                  # Claude Code skill (alwaysApply: true, SessionStart hook)
agents/
├── dreamcontext-initializer.md  # Sub-agent for bootstrapping _dream_context/
├── dreamcontext-explore.md      # Sub-agent for context-first codebase exploration
├── sleep-tasks.md               # Sleep specialist: task files (always fires)
├── sleep-state.md               # Sleep specialist: core 0-6 + CHANGELOG + RELEASES (always fires)
└── sleep-product.md             # Sleep specialist: knowledge + feature PRDs (conditional)
skill-packs/                  # Optional installable skill packs
├── catalog.json              # Master manifest (packs + standalone skills + agents)
├── engineering/              # Pack: coding-principles (alwaysApply) + sub-skills
├── design/                   # Pack: design-principles (alwaysApply) + sub-skills
├── growth/                   # Pack: app-growth (non-alwaysApply) + sub-skills
├── brand-voice/              # Pack: brand-voice-enforcement + sub-skills + references
├── system-prompts/           # Standalone skill: cognitive architecture, prompt engineering
└── agents/                   # 6 optional agents (flat, matching .claude/agents/ layout)
src/templates/init/           # Template files for dreamcontext init (5.data_structures.sql removed in v0.4.0; replaced by data-structures/default.md)
dist/dashboard/               # React SPA (copied from dashboard/dist/ on build)
desktop/                      # Tauri 2.x native shell (v0.6.0; compile-checked; window launch = manual)
└── src-tauri/src/lib.rs      # Spawns Node CLI on free loopback port; loads via WebviewUrl::External; shell capability scoped to 'node'
```

## Key Dependencies

**CLI runtime** (all externalized from bundle):
- `commander@^13.1.0` — CLI argument parsing and subcommand structure
- `chalk@^5.4.1` — Terminal colors. v5+ is ESM-only.
- `gray-matter@^4.0.3` — YAML frontmatter parsing and serialization.
- `@inquirer/prompts@^7.10.1` — Interactive prompts.
- `fast-glob@^3.3.3` — File system glob matching.
- `nanoid@^5.1.2` — Unique ID generation.

**Dashboard runtime** (separate `dashboard/package.json`, not in CLI bundle):
- `react@^19` + `react-dom@^19` — UI framework
- `@tanstack/react-query@^5` — Server state management
- `vite@^6` + `@vitejs/plugin-react@^4` — Build tool and dev server
- `react-force-graph-2d@^1.29.1` — 2D Brain graph canvas (existing)
- `react-force-graph-3d@^1.29.1` + `three@^0.184.0` — 3D Brain graph (lazy-loaded chunk, ~334 KB gzip; only downloaded when user switches to 3D view)
- `mermaid@^11.14.0` — Flowchart rendering in MarkdownPreview and TaskDetailPanel (shared useMermaidRender hook in dashboard/src/lib/mermaidRender.ts)
- `marked@^15.0.0` + `panzoom@^9.4.4` — Markdown rendering and pan/zoom in dashboard

**Dev**:
- `tsup@^8.4.0` — Bundles CLI + server to `dist/index.js`. Copies `dashboard/dist/` to `dist/dashboard/` via `onSuccess`.
- `typescript@^5.7.3` — Strict mode.
- `vitest@^3.0.6` — Test runner (unit + integration).
- `@types/node@^22.13.4` — Node.js type definitions.

## Build Pipeline

```
npm run build
  └─ build:dashboard   →  vite build in dashboard/ → dashboard/dist/
  └─ build:cli         →  tsup src/cli/index.ts + src/server/ → dist/index.js
                           onSuccess: cp dashboard/dist/ → dist/dashboard/
```

## Infrastructure

- **Distribution**: npm public registry (`dreamcontext`). Install: `npm install -g dreamcontext` or `curl | sh` (install.sh in package; v0.5.0). Manual publish via `npm publish`.
- **Build output**: `dist/` (ESM `dist/index.js` + `dist/templates/` + `dist/agents/` + `dist/dashboard/`).
- **Dashboard**: Served locally on port 4173 (configurable, --host for LAN opt-in). Not daemonized.
- **Testing**: vitest. Integration tests run against compiled binary. ~962 tests passing; 1 pre-existing flake (marketing-council.test.ts, non-deterministic).
