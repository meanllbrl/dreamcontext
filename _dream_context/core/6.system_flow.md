---
name: System Flow
type: reference
summary: Session lifecycle, data flows, neuroscience mapping, and consolidation protocol; federation (cross-vault recall + digest inbox + sleep-federation specialist) wired in; versioned migrations (registry+ledger); SessionStart seeds taxonomy + injects setup-drift directive
updated: "2026-06-13"
---

# System Flow: How the Agent Memory System Works

## Design Rationale

Inspired by a 2025 Science paper (Joo & Frank) revealing the brain's two-stage memory selection system. The hippocampus records experiences AND fires "awake ripples" to bookmark important moments during the day. During sleep, bookmarked memories compete for consolidation, with only the strongest patterns winning transfer to the neocortex. Key insight: memory selection (tagging) and memory consolidation (storage) are separate processes.

## Session Lifecycle

```
SESSION START
  SessionStart Hook fires
    -> Seed core/taxonomy.json crash-safely if missing (pre-feature installs)
    -> Analyze unscored sessions (transcript-based)
    -> Generate snapshot:
       Soul, User, Memory (full)
       Extended Core Files index
       Active Tasks (Why capped 250 chars; template comments stripped)
       Bookmarks (tagged moments, sorted by salience)
       Contextual Reminders (triggers matching active tasks)
       Recent Changelog (summary field if present, else description; 5 entries), Latest Release
       Features Summary
       Knowledge Index: pinned files listed first with prominent "MUST READ if relevant" callout;
         all entries show slug + description + tags + staleness; body fetched on demand via Read or recall
       Warm Knowledge (recently accessed, first paragraph)
       [Sleep State removed — hook delivers debt via UserPromptSubmit on every prompt]
       Never-evict tier: setupVersion drift directive (src/lib/setup-drift.ts; stale -> run
         `dreamcontext update`, downgrade -> warn-only; opt-out DREAMCONTEXT_DRIFT_CHECK=0)
       Applied-migration notices (one-line, read from .sleep.json pendingMigrationNotices)
    -> Consolidation directive if:
       debt >= 10, debt >= 7, critical bookmarks exist, 5+ sessions since sleep

ACTIVE WORK
  Agent works on tasks, writes code, makes decisions
    Regular: tasks log, core changelog add, Edit soul/user/memory, knowledge create
    Bookmarking: bookmark add "<message>" -s <1|2|3>
    Access tracking: knowledge touch <slug> (after reading knowledge files)
    Query-time recall (v0.4.0+): memory recall "<query>" (BM25 over knowledge + features + tasks + memory.md LIFO sections)
      Live corpus scan; no persistent index. Use as first-line lookup before grep/glob.
      Hook-injected variant: UserPromptSubmit hook auto-injects top-3 hits per prompt (on by default; opt-out via DREAMCONTEXT_MEMORY_HOOK=0).

SESSION END
  Stop Hook fires
    -> Analyze transcript (change_count, tool_count)
    -> Score = max(changeScore, toolScore) -> 0-3
    -> Link unlinked bookmarks to session
    -> Increment sessions_since_last_sleep
    -> Store session record (LIFO), add score to debt

CONSOLIDATION (REM SLEEP)
  Triggered by: debt >= 7, critical bookmarks, 5+ sessions, task completion
  Protocol:
    0. sleep start (set epoch; runs pending versioned migrations as a safety net —
       registry src/migrations/, ledger state/.migrations.json; primary run is on
       `dreamcontext update`. STRUCTURE-only migrations route to agents/sleep-migration.md
       via `dreamcontext migrations pending|record`)
    1. Read bookmarks FIRST (salience order), then session summaries
       For important sessions: transcript distill <session_id>
    2. Decision tree (what to update)
    3. Execute updates + create triggers for context-dependent decisions
    4. Anti-bloat (~150-line limit per core file; v0.4.0+; tightened from 300 because memory recall fetches archived content on demand)
    4a. Federation inbox drain (sleep-federation specialist, runs if federation is active): `federation drain` ingests pending entries from state/.federation-inbox/, resolves slug collisions, creates conflict bookmarks; `federation sync` (dry-run by construction) pushes relevance-filtered digests to connected vaults.
    5. Feature consolidation (task -> feature PRD; trigger: >=2 AC, user named it, or dangling feature: frontmatter)
    6. sleep done "<summary>"
       Clears pre-epoch sessions/bookmarks/changes
       Expires triggers past max_fires
       Writes sleep_history entry
       Resets sessions_since_last_sleep to 0
    7. Report back
```

## Neuroscience Mapping

| Brain Mechanism | dreamcontext Feature |
|---|---|
| Hippocampus (working memory) | state/*.md (active tasks) |
| Neocortex (long-term storage) | core/ files, knowledge/ files |
| Awake sharp wave ripples | bookmark command (tag moments for consolidation) |
| Neural competition (strongest win) | Salience scoring (critical bookmarks trigger consolidation) |
| Memory decay (unused synapses weaken) | Knowledge access tracking (staleness indicators) |
| Sleep rhythm (every night) | Session count rhythm (advisory every 5 sessions) |
| Spreading activation | Warm knowledge tier (recently accessed = partial load) |
| Prospective memory ("do X when Y") | Contextual triggers (remind about X when task Y) |
| Hippocampal filtering | Transcript distillation (structural filter for signal) |
| Sleep consolidation | REM sleep agent (compressed replay -> core files) |
| Inhibitory neurons | Anti-bloat pass (200-line limit, prune stale) |
| Synaptic plasticity | LIFO ordering + Edit/Write (recent info surfaces first) |

## Data Flows

**Bookmark flow**: Agent calls `bookmark add` -> stored in .sleep.json bookmarks[] -> stop hook links to session -> sleep agent reads first -> consolidated into soul/user/memory/knowledge

**Knowledge decay flow**: Agent reads knowledge file -> calls `knowledge touch <slug>` -> .sleep.json knowledge_access updated -> snapshot shows staleness indicators + warm knowledge tier -> sleep agent uses access data for anti-bloat

**Trigger flow**: Sleep agent creates trigger -> stored in .sleep.json triggers[] -> snapshot matches against active tasks/tags/bookmarks -> matching triggers shown as contextual reminders -> fired_count incremented -> expired by sleep done when past max_fires

**Transcript distillation flow**: Sleep agent calls `transcript distill <session_id>` -> reads JSONL transcript -> auto-filters to content after `consolidated_at` if session was previously consolidated (prevents re-reading already-processed content) -> structural filter keeps user messages, thinking blocks, agent decisions, Write/Edit calls with byte deltas, Bash changes, errors, bookmarks, subagent I/O -> discards Read/Glob/Grep results, tool metadata, internal subagent tool calls -> outputs structured markdown. `--full` shows entire transcript; `--since <iso>` for manual cutoff.

## Salience Levels

| Level | When to Use | Consolidation Effect |
|---|---|---|
| ★ (1) | Notable decision, useful pattern | Consolidate if relevant |
| ★★ (2) | Architectural decision, user preference, significant bug | Should consolidate |
| ★★★ (3) | Critical constraint, breaking change, fundamental design choice | Must consolidate (triggers immediate advisory) |

## Consolidation Triggers

| Condition | Directive |
|---|---|
| Debt >= 10 | Strong recommendation |
| Critical (★★★) bookmarks exist | Critical bookmark advisory (regardless of debt) |
| Debt >= 7 | Elevated advisory |
| 5+ sessions since last sleep | Rhythm check advisory |

## Key File Paths

| Component | Path |
|---|---|
| Sleep state (all runtime data) | _dream_context/state/.sleep.json |
| Sleep history (consolidation log) | _dream_context/state/.sleep-history.json |
| Core files (long-term memory) | _dream_context/core/0-6.* |
| Data structures (single-product) | _dream_context/knowledge/data-structures/default.md |
| Data structures (multi-product) | _dream_context/knowledge/data-structures/<product>.md |
| System flow reference | _dream_context/core/6.system_flow.md |
| Feature PRDs | _dream_context/core/features/*.md |
| Active tasks | _dream_context/state/*.md |
| Install manifest | _dream_context/state/.install-manifest.json |
| Migration ledger (per-clone) | _dream_context/state/.migrations.json |
| Tag taxonomy (faceted vocabulary) | _dream_context/core/taxonomy.json |
| Platform/product config | _dream_context/state/.config.json |
| Knowledge files | _dream_context/knowledge/*.md |
| Per-product knowledge | _dream_context/knowledge/products/<product>.md |
| Session transcripts | ~/.claude/projects/*/JSONL files |
| Skill definition | .claude/skills/dreamcontext/SKILL.md |
| Sleep specialists | .claude/agents/sleep-{tasks,state,product,migration}.md |
| Initializer agent | .claude/agents/dreamcontext-initializer.md |

## SleepState Schema

> Full schema: `_dream_context/state/.sleep.json` (debt, sessions[], bookmarks[], triggers[], knowledge_access, dashboard_changes). History log: `state/.sleep-history.json` (consolidated_at, session_ids — used by `transcript distill` auto-filter). See `src/cli/commands/sleep.ts` for canonical shape.
