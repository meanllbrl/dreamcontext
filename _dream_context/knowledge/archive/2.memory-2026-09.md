---
name: 2.memory-2026-09
type: knowledge
tags: [archive, decisions]
pinned: false
---

# Archived Technical Decisions — 2026-09

Extracted from `core/2.memory.md` during sleep-state consolidation (2026-09-05/06) to keep the core file under its anti-bloat ceiling. Findable via `dreamcontext memory recall`.

- **★★ Component reuse over reimplementation (2026-08-26)**: mount existing atomic components on new session objects rather than rebuilding. A hand-rolled composer costs slash menu, attachments, triggers, queue, drafts, and drifts on change. Source: peer-mail panel initially rebuilt textarea+Send, Allow/Deny, transcript while Composer/PermissionCard/ItemView existed. Mounting is cheaper, complete, stays current.

- **★★ Presence assertions pass on invisible elements — assert geometry (2026-08-26)**: a DOM presence check (element exists, :visible, text queryable) passes on an element rendered INVISIBLY. The peer panel rendered inside the docked strip with position:absolute/inset:0, so it resolved against a ~40px box — in the DOM, ':visible', text queryable, and nothing on screen. The screenshot caught it; the test did not. Fix: assert GEOMETRY (boundingBox) for anything that must be seen. Playwright's toBeVisible() checks computed visibility, not layout — it passed while the element was off-screen. Source: scripts/verify/peer-mail-ui.mjs §6a.
