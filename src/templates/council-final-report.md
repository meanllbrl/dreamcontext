---
debate_id: "{{ID}}"
topic: "{{TOPIC}}"
synthesized_at: "{{DATE}}"
personas: {{PERSONAS}}
rounds: {{ROUNDS}}
---

## Verdict

(The decision in one or two sentences.)

## Why

(Every WHY traced. One bullet per reason, 1–2 sentences human prose. Source tag
as a trailing italic line — never mid-sentence parentheticals. If a whole section
shares the same source mix, write `*Consensus: slug R1, slug R2*` once at the
section top instead of repeating per bullet.)

## What was debated

(Ideas, pushback, position shifts across rounds. Name personas; cite round numbers.)

## Minority views

(Positions that did not win but deserve recording — especially risks the majority dismissed.)

## Open risks

(What could still go wrong if the verdict is acted on. Flag concrete monitoring or mitigations.)

## Appendix: per-agent per-round summaries

(Executive summaries only — one per persona per round.)
