---
persona: "{{SLUG}}"
rounds_completed: 0
---

<!-- Each round is appended above previous rounds (LIFO). Required subsections per round: Executive Summary, Position, Reasoning, Reactions to peers, Open questions. Optional: ### Cross-examination (answers to directives queued via `council question`). Before appending, record your verdict: `council verdict <debate_id> <slug> <round> --stance <s> --conviction <0-100> --headline "..."` — report append warns when the round has no verdict. -->
